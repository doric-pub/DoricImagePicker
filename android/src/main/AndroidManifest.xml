<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pub.doric.android.library.doricimagepicker">

    <application>
        <provider
            android:name="pub.doric.library.ImagePickerFileProvider"
            android:authorities="${applicationId}.doric.image_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/doric_image_picker_file_paths" />
        </provider>
    </application>

</manifest>
